#include<stdio.h>
int main()
{
    float n,sum;
    printf("enter value of n");
    scanf("%f",&n);
    sum=(n*n+n)/2;
    printf("sum of the first one hundred numbers:%f",sum);
    return (0);
}